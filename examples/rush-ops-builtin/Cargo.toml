[package]
name = "rush-ops-builtin"
version = "0.1.0"
edition = "2021"
description = "Built-in Rust ops for the Hush workflow engine (math, string, JSON, hash)"

[lib]
crate-type = ["cdylib"]

[dependencies]
rush-ops-sdk = { path = "../../rush-core/sdk" }
serde_json = "1"
