[project]
name = "operonx-ex10-multi-model"
version = "0.1.0"
description = "Operonx multi-model patterns — parallel, routing, load-balance, fallback, ensemble."
requires-python = ">=3.10"
dependencies = [
    "operonx[openai,serve]",
]

[tool.uv.sources]
operonx = { path = "../../..", editable = true }
