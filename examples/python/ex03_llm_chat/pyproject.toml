[project]
name = "operonx-ex03-llm-chat"
version = "0.1.0"
description = "Operonx LLM chat — basic / chain / summarize via OpenAI."
requires-python = ">=3.10"
dependencies = [
    "operonx[openai,serve]",
]

[tool.uv.sources]
operonx = { path = "../../..", editable = true }
