[project]
name = "operonx-ex14-streaming-tracing"
version = "0.1.0"
description = "Operonx generator pipelines + tracing hooks (no API keys)."
requires-python = ">=3.10"
dependencies = [
    "operonx",
]

[tool.uv.sources]
operonx = { path = "../../..", editable = true }
