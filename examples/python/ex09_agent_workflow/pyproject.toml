[project]
name = "operonx-ex09-agent-workflow"
version = "0.1.0"
description = "Operonx tool-calling agent on @graph with until= loop."
requires-python = ">=3.10"
dependencies = [
    "operonx[openai]>=1.3.0",
]
