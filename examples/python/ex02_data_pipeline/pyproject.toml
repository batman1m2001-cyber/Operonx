[project]
name = "operonx-ex02-data-pipeline"
version = "0.1.0"
description = "Operonx data pipelines — pure compute, no API keys."
requires-python = ">=3.10"
dependencies = [
    "operonx[serve]",
]

[tool.uv.sources]
operonx = { path = "../../..", editable = true }
