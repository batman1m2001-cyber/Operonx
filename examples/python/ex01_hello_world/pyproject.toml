[project]
name = "operonx-ex01-hello-world"
version = "0.1.0"
description = "Operonx hello-world — three tiny graphs, no API keys."
requires-python = ">=3.10"
dependencies = [
    "operonx>=0.6.2",
]
