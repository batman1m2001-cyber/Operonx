[project]
name = "operonx-ex11-parallel-advanced"
version = "0.1.0"
description = "Operonx fan-out/fan-in, generator iteration, partial failure."
requires-python = ">=3.10"
dependencies = [
    "operonx>=1.3.0",
]
