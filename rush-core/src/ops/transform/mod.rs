// Transform ops module — previously contained func_op.rs (built-in registry).
// Built-in Rust ops are now in the rush-ops-builtin cdylib plugin.
// Plugin ops are loaded at runtime via rush-core/src/plugins/.
